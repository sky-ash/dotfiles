# currently facing issue that shaders in ~/.config/hypr/shaders/ dont seem to be recognized. 
# instead only /usr/share/hyprshade/shaders is taken into account.  

[[shades]]
name = "vibrance"
default = true  # will be activated when no other shader is scheduled

[[shades]]
name = "blue-light-filter"
start_time = 19:00:00
end_time = 06:00:00   # optional if more than one shader has start_time

[[shades]]
name = "reduce-brightness"
start_time = 22:00:00
end_time = 07:00:00